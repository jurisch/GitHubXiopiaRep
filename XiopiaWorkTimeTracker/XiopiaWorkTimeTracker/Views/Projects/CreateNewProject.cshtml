@using Ressources
@using XiopiaWorkTimeTracker.Models.Database;

@model XiopiaWorkTimeTracker.Models.Database.Project

@{
    ViewBag.Title = @Language.newProject;
}

<h2>@Language.newProject</h2>
<script src="~/Scripts/jquery-1.10.2.min.js"></script>
@*<script type="text/javascript">
    $(document).ready(function () {
    	//GetEmployees();
    });
    function GetEmployees() {
    	$("#EmployeeId").empty();

    	$.ajax({
    		data: "{}",
    		type: "POST",
    		url: '@Url.Action("GetAllEmployeesJson", "Projects")',
    		dataType: "json",
    		contentType: "application/json; charset=utf-8",
    		success: function (result) {
    			//$.each(result, function (index, employee) {
    			//	$("#EmployeeId").append("<option value=" + employee.Id + ">" + employee.Account + "</option>");
    			//});
    			alert([result.d]);
    		},
    		error: function (xhr, ajaxOptions, thrownError) {
    			alert(thrownError);
    		}
    	});
    }
</script>*@

@using (Html.BeginForm()) 
{
    @Html.AntiForgeryToken()
    
    <div class="form-horizontal">
        <h4>Project</h4>
        <hr />
        @Html.ValidationSummary(true, "", new { @class = "text-danger" })
        <div class="form-group">
            @Html.LabelFor(model => model.Name, htmlAttributes: new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.EditorFor(model => model.Name, new { htmlAttributes = new { @class = "form-control" } })
                @Html.ValidationMessageFor(model => model.Name, "", new { @class = "text-danger" })
            </div>
        </div>

		<div class="form-group">
			@Html.LabelFor(model => model.ProjectResponsibleId, htmlAttributes: new { @class = "control-label col-md-2" })
			<div class="col-md-10">
				@Html.DropDownListFor(model => model.ProjectResponsibleId, Model.Employees.Select(m => new SelectListItem
																			   {
																				   Text = m.FirstName +" "+ m.LastName,
																				   Value = m.Id.ToString()
																			   })  ,"Please select a Member")
				@Html.ValidationMessageFor(model => model.ProjectResponsibleId, "", new { @class = "text-danger" })
			</div>
		</div>

		<div class="form-group">
			<div class="col-md-offset-2 col-md-10">
				<input type="submit" value="Create" class="btn btn-default" />
			</div>
		</div>
	</div>
}


<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
