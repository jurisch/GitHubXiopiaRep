@using Ressources
@*@model IEnumerable<XiopiaWorkTimeTracker.Models.Database.Project>*@
@model XiopiaWorkTimeTracker.Models.ViewModels.ProjectViewModel

@{
	ViewBag.Title = "Projekt Verwaltung";
}

<script src="~/Scripts/jquery-1.10.2.min.js"></script>

<h2>Projekt Verwaltung</h2>
<p>
	<button id="newPrjectBtn" type="button" class="btn btn-primary btn-lg" onclick="location.href='@Url.Action("CreateNewProject", "Projects")'">@Language.newProject</button>
</p>
<table class="table">
	<tr>
		<th>
			@*@Html.DisplayNameFor(Model.projects => Model.projects)*@
			name
		</th>
		<th>
			@*@Html.DisplayNameFor(model => model.ProjectResponsibleId)*@
			member
		</th>
		<th></th>
	</tr>

	@foreach (var item in Model.projects)
	{
		<tr>
			<td>
				@Html.DisplayFor(modelItem => item.Name)
			</td>
			<td>
				@*@Html.Display(item.Employees.Single(e => e.Id == item.Id).FirstName)*@
			</td>
			<td>
				@Html.ActionLink("Edit", "EditProject", new { id = item.Id }) |
				@*Html.ActionLink("Details", "Details", new { id=item.Id }) |*@
				@Html.ActionLink("Delete", "DeleteProject", new { id = item.Id })
			</td>
		</tr>
	}

</table>
